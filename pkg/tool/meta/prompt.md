Ambient information about the agent's runtime and the active workspace. Call `current_time` to anchor relative phrases like "yesterday" or "last week" before issuing other tool calls. Call `workspace_describe` to translate status IDs returned by `ticket_*` tools into human-readable names, or to discover what custom fields tickets carry in this workspace. These tools take no arguments and are cheap; prefer using them once when needed rather than repeatedly inside a loop.
