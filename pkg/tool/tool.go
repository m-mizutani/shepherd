// Package tool defines the ToolFactory abstraction (factory.go) and the
// Catalog that gates factories per workspace (catalog.go). Concrete factories
// live in subpackages: notion, slack, ticket, meta.
package tool
